type:
  name: std::shared_ptr<Controller>
  include: "src/control/controller.h"

---

component:
  name: ShipControl
  members:
    - controller: std::shared_ptr<Controller>

---

system:
  name: Control
  families:
  - main:
    - ShipControl: read
    - Body: write
  strategy: individual
  services:
    - ControllerService